import { Outlet, Link } from "react-router-dom"
import Card from "../Card"

function Home({todos}){
    return(
        <div>
            {/* <Card todos={todos}/> */}
        </div>
    )
}

export default Home